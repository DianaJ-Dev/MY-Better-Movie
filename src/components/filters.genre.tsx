/*import React, { useState, useEffect } from 'react';
import fetch from 'node-fetch';
import './filters.genre.module.css';



export const FiltrosByGenre = () => {
  

  return (
    <div>
      <select name="genre">
        {genres.map(elemento => (
          <option key={elemento.id} value={elemento.name}>
            {elemento.name}
          </option>
        ))}
      </select>
    </div>
  );
};*/